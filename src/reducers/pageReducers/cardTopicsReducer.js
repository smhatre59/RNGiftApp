import * as types from '../../constants/actionTypes';
import initialState from '../initialState.js';
export default function (state = initialState.projectTopics, action) {
	switch (action.type) {
		default:
			return state;
	}
}
